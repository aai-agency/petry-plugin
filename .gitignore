# local insight store generated by petry-insights in a user's project — never the plugin's own
.petry/

# deps / build (plugins ship source; none of this belongs in the repo)
node_modules/
dist/

# OS / editor
.DS_Store
*.log
