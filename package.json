{
  "name": "petry-plugin",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "test": "node --test",
    "check": "node --check scripts/capture.mjs && node --check scripts/render-preview.mjs && node --test && pnpm --dir build check"
  }
}
